# Run the server: pip install -r requirements.txt && uvicorn server.app:app --reload --port 8000
# (scripts/train_qlora.py, fetch_ben_images.py, eval_bench.py carry their own pip lines for Kaggle -- not needed here)
fastapi
uvicorn[standard]
python-multipart
pydantic
numpy
pillow
rasterio
scipy
scikit-image
torch
transformers>=4.57
peft
bitsandbytes
accelerate
